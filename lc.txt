commit da7e45a45f094b2dab525874f9d2571c8e81b5d1 Author: uhmmokay <uhmmmokay@outlook.com> Date: Sun Aug 2 00:24:08 2026 +0300 Removed "cryptcat" support, added "Ncat" support!! Yay!! diff --git a/.gitignore b/.gitignore index 1b08578..1bd9670 100644 --- a/.gitignore +++ b/.gitignore @@ -144,4 +144,5 @@ ipch/ # Visual Studio Trace Files *.e2e -.vscode \ No newline at end of file +.vscode +cchat diff --git a/README.md b/README.md index dc82f3c..8cd281a 100644 --- a/README.md +++ b/README.md @@ -1,7 +1,7 @@ -<h1> Cchat v2 - Cryptcat Assistant for GNU/Linux Distributions</h1> +<h1> Cchat v3 - Ncat TUI for GNU/Linux Distributions</h1> -#### Rejoice - you no longer have to struggle with flags to get cryptcat working properly in your terminal! -#### Here is an open-source assistant written in C++ for high performance. +#### Rejoice - you no longer have to struggle with flags to get ncat working properly in your terminal! +#### Here is an open-source TUI app written in C++ for high performance. <h1>Installation</h1> @@ -17,7 +17,7 @@ g++ ./src/*.cpp -o cchat <h1> So, what exactly is this program? </h1> -#### It solves the problem of users having to enter flags to use cryptcat. +#### It solves the problem of users having to enter flags to use ncatcat. #### Currently, the program implements only two functions: diff --git a/src/chat.cpp b/src/chat.cpp index a889727..ddb7408 100644 --- a/src/chat.cpp +++ b/src/chat.cpp @@ -3,31 +3,31 @@ #include <string> -void createRoom(std::uint16_t port, std::string pass) +void createRoom(std::uint16_t port) { if (port < 1023) { std::system("sudo -v"); - std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port) + " [SYSTEM], pass: " + pass; + std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port) + " [SYSTEM]"; std::system(x.c_str()); std::cout << "Press enter to start chat:
"; ignore(); std::system("clear"); - std::string createRoomCommand = "sudo cryptcat -l -p " + std::to_string(port) + " -k " + escapeForShell(pass); + std::string createRoomCommand = "sudo ncat -l " + std::to_string(port) + " -k "; std::system(createRoomCommand.c_str()); } else { - std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port) + ", pass: " + pass; + std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port); std::system(x.c_str()); std::cout << "Press enter to start chat:
"; ignore(); std::system("clear"); - std::string createRoomCommand = "cryptcat -l -p " + std::to_string(port) + " -k " + escapeForShell(pass); + std::string createRoomCommand = "ncat -l " + std::to_string(port); std::system(createRoomCommand.c_str()); } } -void connectTo(std::string ip, std::uint16_t port, std::string pass) +void connectTo(std::string ip, std::uint16_t port) { - std::string connectTOCommand = "clear && cryptcat -k " + escapeForShell(pass) + " " + escapeForShell(ip) + " " + std::to_string(port); + std::string connectTOCommand = "clear && ncat " + escapeForShell(ip) + " " + std::to_string(port); std::system(connectTOCommand.c_str()); } diff --git a/src/chat.h b/src/chat.h index eb7705f..2f59160 100644 --- a/src/chat.h +++ b/src/chat.h @@ -1,5 +1,5 @@ #pragma once #include "default.h" -void createRoom(std::uint16_t port, std::string pass); -void connectTo(std::string ip, std::uint16_t port, std::string pass); \ No newline at end of file +void createRoom(std::uint16_t port); +void connectTo(std::string ip, std::uint16_t port); \ No newline at end of file diff --git a/src/default.cpp b/src/default.cpp index 9ea5947..e3d0d03 100644 --- a/src/default.cpp +++ b/src/default.cpp @@ -26,16 +26,10 @@ std::uint16_t getPort() std::string getPwd() { - std::string pwd {getString("Enter path (example: /home/user/target/directory): ", "Invalid path. Exiting...
")}; + std::string pwd {getString("Enter path (example: /home/user/target/directory/file.txt): ", "Invalid path. Exiting...
")}; return pwd; } -std::string getPass() -{ - std::string pass {getString("Enter Secret Key: ", "Invalid value. Exiting...")}; - return pass; -} - std::string escapeForShell(const std::string& str) { std::string escaped = "'"; for (char c : str) { @@ -87,7 +81,7 @@ std::string getString(std::string msg, std::string errorMsg) void introduction() { std::system("clear"); - std::cout << "Hello! This is a 'cchat'. 
[32mYou can get more documentation on 'https://github.com/uhmmokay/cchat'.[0m
This program uses 'cryptcat'
"; + std::cout << "Hello! This is a 'cchat'. 
[32mYou can get more documentation on 'https://github.com/uhmmokay/cchat'.[0m
This program uses 'ncat'
"; } std::uint16_t whatDoYouWant() diff --git a/src/default.h b/src/default.h index 11e9267..172ca85 100644 --- a/src/default.h +++ b/src/default.h @@ -7,7 +7,6 @@ void introduction(); std::uint16_t whatDoYouWant(); std::string getip(); std::uint16_t getPort(); -std::string getPass(); std::string escapeForShell(const std::string& str); void ignore(); std::uint16_t getValue(std::string msg, std::string errorMsg); diff --git a/src/file.cpp b/src/file.cpp index 17ceb46..25c0c01 100644 --- a/src/file.cpp +++ b/src/file.cpp @@ -2,27 +2,24 @@ #include "default.h" #include <string> -void createFileListener(std::string ip, std::string pass, std::uint16_t port, std::string toDir) +void createFileListener(std::string ip, std::uint16_t port, std::string toDir) { - std::string cFL = "clear && echo 'Sending...' && cryptcat -k " - + escapeForShell(pass) - + " -w 3 " + std::string cFL = "clear && echo 'Sending...' && ncat " + escapeForShell(ip) + " " + std::to_string(port) + " < " + escapeForShell(toDir); std::system(cFL.c_str()); } -void createFileDropper(std::string pass, std::uint16_t port, std::string Dir) +void createFileDropper(std::uint16_t port, std::string Dir) { - std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port) + ", pass: " + pass; + std::string x = "clear && echo Your ip is $(curl ifconfig.me), your port is " + std::to_string(port); std::system(x.c_str()); std::cout << "Press enter...
"; ignore(); - std::string cFD = "clear && echo 'Dropping file...' && cryptcat -l -p " + std::string cFD = "clear && echo 'Dropping file...' && ncat -l " + std::to_string(port) - + " -k " + escapeForShell(pass) + " > " + escapeForShell(Dir); std::system(cFD.c_str()); } diff --git a/src/file.h b/src/file.h index f32b970..0804d6a 100644 --- a/src/file.h +++ b/src/file.h @@ -1,5 +1,5 @@ #pragma once #include "default.h" -void createFileListener(std::string ip, std::string pass, std::uint16_t port, std::string toDir); -void createFileDropper(std::string pass, std::uint16_t port, std::string Dir); \ No newline at end of file +void createFileListener(std::string ip, std::uint16_t port, std::string toDir); +void createFileDropper(std::uint16_t port, std::string Dir); \ No newline at end of file diff --git a/src/main.cpp b/src/main.cpp index 037a1ac..8d6ac0f 100644 --- a/src/main.cpp +++ b/src/main.cpp @@ -12,29 +12,28 @@ int main () case 0: { std::uint16_t port {getPort()}; - std::string pass {getPass()}; - createRoom(port, pass); + createRoom(port); break; } case 1: { - std::string ip {getip()}, pass {getPass()}; + std::string ip {getip()}; std::uint16_t port {getPort()}; - connectTo(ip, port, pass); + connectTo(ip, port); break; } case 2: { - std::string ip {getip()}, pass {getPass()}, toDir {getPwd()}; + std::string ip {getip()}, toDir {getPwd()}; std::uint16_t port {getPort()}; - createFileListener(ip, pass, port, toDir); + createFileListener(ip, port, toDir); break; } case 3: { - std::string pass {getPass()}, Dir {getPwd()}; + std::string Dir {getPwd()}; std::uint16_t port {getPort()}; - createFileDropper(pass, port, Dir); + createFileDropper(port, Dir); break; } }
